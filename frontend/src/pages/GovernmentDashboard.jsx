import { useState } from 'react';
import { getComplaints, saveComplaints } from '../services/storage';
export default function GovernmentDashboard(){const [items,setItems]=useState(getComplaints());const update=(id,status)=>{const next=items.map(c=>c.id===id?{...c,status}:c);saveComplaints(next);setItems(next)};return <main className="page"><div className="page-heading"><div><div className="eyebrow">AUTHORITY CONSOLE</div><h1>Government dashboard</h1><p>Demo view for municipal officers and road maintenance teams.</p></div><span className="role-pill">Officer preview</span></div><div className="stats"><div className="stat-card"><div className="stat-icon">📁</div><div><p>Total</p><h3>{items.length}</h3></div></div><div className="stat-card"><div className="stat-icon">⏳</div><div><p>Pending</p><h3>{items.filter(c=>c.status==='Submitted').length}</h3></div></div><div className="stat-card"><div className="stat-icon">🔧</div><div><p>Progress</p><h3>{items.filter(c=>c.status==='In Progress').length}</h3></div></div><div className="stat-card"><div className="stat-icon">✓</div><div><p>Resolved</p><h3>{items.filter(c=>c.status==='Resolved').length}</h3></div></div></div><div className="table-wrap"><table><thead><tr><th>ID</th><th>Hazard</th><th>Severity</th><th>Department</th><th>Status</th><th>Action</th></tr></thead><tbody>{items.map(c=><tr key={c.id}><td>{c.id}</td><td>{c.hazard}</td><td><span className={`severity ${c.severity.toLowerCase()}`}>{c.severity}</span></td><td>{c.department}</td><td><span className="badge">{c.status}</span></td><td><select value={c.status} onChange={e=>update(c.id,e.target.value)}><option>Submitted</option><option>Assigned</option><option>In Progress</option><option>Resolved</option></select></td></tr>)}</tbody></table>{!items.length&&<div className="empty"><h3>No incoming complaints</h3><p>Submit a report from the citizen flow first.</p></div>}</div></main>}
