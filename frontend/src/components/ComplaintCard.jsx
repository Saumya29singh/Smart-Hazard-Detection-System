import { Link } from 'react-router-dom';
export default function ComplaintCard({complaint}){return <div className="complaint-card"><div className="complaint-top"><span className="id">{complaint.id}</span><span className={`badge ${complaint.status.toLowerCase().replaceAll(' ','-')}`}>{complaint.status}</span></div><h3>{complaint.hazard}</h3><p>{complaint.description}</p><div className="complaint-meta"><span>📍 {complaint.latitude ? `${Number(complaint.latitude).toFixed(4)}, ${Number(complaint.longitude).toFixed(4)}` : 'Location unavailable'}</span><span className={`severity ${complaint.severity.toLowerCase()}`}>{complaint.severity} severity</span></div><Link className="text-link" to={`/complaints/${complaint.id}`}>View details →</Link></div>}
